import 'package:test/test.dart';
import 'package:mastodon/mastodon.dart';

// tests for InstanceConfigurationVapid
void main() {
  final InstanceConfigurationVapid?
  instance = /* InstanceConfigurationVapid(...) */ null;
  // TODO add properties to the entity

  group(InstanceConfigurationVapid, () {
    // The server's VAPID public key, used for push notifications, the same as [WebPushSubscription#server_key](https://docs.joinmastodon.org/entities/WebPushSubscription/#server_key).
    // String publicKey
    test('to test the property `publicKey`', () async {
      // TODO
    });
  });
}
